import {Router} from 'express'; import {list,getBySlug,create,update,remove} from '../controllers/product.controller.js'; import {protect,requireRoles} from '../middleware/auth.js'; const r=Router(); r.get('/',list); r.get('/:slug',getBySlug); r.post('/',protect,requireRoles('admin','superadmin'),create); r.patch('/:id',protect,requireRoles('admin','superadmin'),update); r.delete('/:id',protect,requireRoles('admin','superadmin'),remove); export default r;
