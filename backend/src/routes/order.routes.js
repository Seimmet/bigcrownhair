import {Router} from 'express'; import {create,mine,all,updateStatus} from '../controllers/order.controller.js'; import {protect,requireRoles} from '../middleware/auth.js'; const r=Router(); r.post('/',protect,create); r.get('/mine',protect,mine); r.get('/admin/all',protect,requireRoles('admin','superadmin'),all); r.patch('/:id/status',protect,requireRoles('admin','superadmin'),updateStatus); export default r;
