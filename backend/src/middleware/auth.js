import jwt from 'jsonwebtoken'; import User from '../models/User.js'; export async function protect(req,res,next){try{const t=req.cookies.token||(req.headers.authorization?.startsWith('Bearer ')?req.headers.authorization.slice(7):null);if(!t)return res.status(401).json({message:'Authentication required'});const d=jwt.verify(t,process.env.JWT_SECRET),u=await User.findById(d.sub).select('-password');if(!u||!u.isActive)return res.status(401).json({message:'Invalid session'});req.user=u;next()}catch{return res.status(401).json({message:'Invalid or expired token'})}} export const requireRoles=(...roles)=>(req,res,next)=>roles.includes(req.user.role)?next():res.status(403).json({message:'Insufficient permissions'});
