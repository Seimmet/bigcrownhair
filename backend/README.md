# BIGCROWN Hair Backend

Express + MongoDB + JWT ecommerce API for the uploaded BIGCROWN Lovable storefront.

```bash
npm install
cp .env.example .env
npm run seed:admin
npm run seed:demo
npm run dev
```

Swagger: http://localhost:5000/api/docs

Roles: customer, admin, superadmin.
