import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { Plus, UserCog, X } from "lucide-react";
import { toast } from "sonner";
import { useCreateStaff, useCurrentUser, useAdminStaff } from "@/hooks/useAdmin";
import { PageTitle } from "@/components/admin/AdminShell";
export const Route=createFileRoute("/admin/staff")({component:Staff});
function Staff(){const {data:user}=useCurrentUser();const {data=[],isLoading}=useAdminStaff();const create=useCreateStaff();const [open,setOpen]=useState(false);const [form,setForm]=useState({name:"",email:"",password:"",role:"admin"});const save=async(e:any)=>{e.preventDefault();try{await create.mutateAsync(form);toast.success("Staff account created");setOpen(false);setForm({name:"",email:"",password:"",role:"admin"})}catch(err:any){toast.error(err?.response?.data?.message||"Could not create staff")}};return <><PageTitle title="Staff" description="Manage the people who can access the admin console." action={user?.role==="superadmin"?<button onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-xl bg-purple-600 px-4 py-3 text-sm font-bold text-white"><Plus className="size-4"/> Add staff</button>:undefined}/>{user?.role!=="superadmin"&&<div className="mb-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">Only superadmins can create or manage staff accounts.</div>}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{isLoading?<div>Loading…</div>:data.map((s:any)=><div key={s._id} className="rounded-2xl border bg-white p-5"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-full bg-purple-100 text-purple-700"><UserCog className="size-5"/></div><div><div className="font-semibold">{s.name}</div><div className="text-xs text-slate-400">{s.email}</div></div></div><div className="mt-5 flex justify-between text-xs"><span className="rounded-full bg-slate-100 px-2.5 py-1 capitalize">{s.role}</span><span className={s.isActive?"text-emerald-600":"text-slate-400"}>{s.isActive?"Active":"Inactive"}</span></div></div>)}</div>{open&&<div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/50 p-4"><form onSubmit={save} className="w-full max-w-md rounded-2xl bg-white p-6"><div className="mb-6 flex justify-between"><h2 className="text-xl font-bold">Create staff account</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="space-y-4">{[["Name","name","text"],["Email","email","email"],["Password","password","password"]].map(([label,key,type])=><label key={key} className="block"><span className="mb-2 block text-sm font-semibold">{label}</span><input required value={(form as any)[key]} type={type} onChange={e=>setForm({...form,[key]:e.target.value})} className="w-full rounded-xl border px-3 py-2.5"/></label>)}<label className="block"><span className="mb-2 block text-sm font-semibold">Role</span><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="w-full rounded-xl border px-3 py-2.5"><option value="admin">Admin</option><option value="superadmin">Superadmin</option></select></label></div><div className="mt-7 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded-xl border px-4 py-2.5">Cancel</button><button className="rounded-xl bg-purple-600 px-5 py-2.5 font-bold text-white">Create account</button></div></form></div>}</>}
