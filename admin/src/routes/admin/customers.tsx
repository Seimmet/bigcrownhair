import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { Search } from "lucide-react";
import { useCustomers } from "@/hooks/useAdmin";
import { PageTitle } from "@/components/admin/AdminShell";
export const Route=createFileRoute("/admin/customers")({component:Customers});
function Customers(){const [search,setSearch]=useState("");const {data=[],isLoading}=useCustomers(search);return <><PageTitle title="Customers" description="Search your BIGCROWN customer base."/><div className="mb-4 flex items-center gap-3 rounded-2xl border bg-white px-4 py-3"><Search className="size-4 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by name or email…" className="w-full bg-transparent text-sm outline-none"/></div><div className="overflow-hidden rounded-2xl border bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-4">Customer</th><th>Phone</th><th>Status</th><th>Joined</th></tr></thead><tbody className="divide-y">{isLoading?<tr><td colSpan={4} className="p-12 text-center text-slate-400">Loading…</td></tr>:data.map((c:any)=><tr key={c._id}><td className="px-5 py-4"><div className="font-semibold">{c.name}</div><div className="text-xs text-slate-400">{c.email}</div></td><td>{c.phone||"—"}</td><td><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${c.isActive?"bg-emerald-50 text-emerald-700":"bg-slate-100 text-slate-500"}`}>{c.isActive?"Active":"Inactive"}</span></td><td className="pr-5 text-xs text-slate-500">{new Date(c.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></div></>}
