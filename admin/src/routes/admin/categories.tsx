import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { Pencil, Plus, X } from "lucide-react";
import { toast } from "sonner";
import { useAdminCategories, useCreateCategory, useUpdateCategory } from "@/hooks/useAdmin";
import { PageTitle } from "@/components/admin/AdminShell";
export const Route = createFileRoute("/admin/categories")({ component: Categories });
function Categories(){const {data=[],isLoading}=useAdminCategories();const create=useCreateCategory();const update=useUpdateCategory();const [open,setOpen]=useState(false);const [editing,setEditing]=useState<any>(null);const [form,setForm]=useState({name:"",slug:"",imageKey:""});const edit=(c:any)=>{setEditing(c);setForm({name:c.name,slug:c.slug,imageKey:c.imageKey||""});setOpen(true)};const save=async(e:any)=>{e.preventDefault();try{if(editing)await update.mutateAsync({id:editing._id,data:form});else await create.mutateAsync(form);toast.success(editing?"Category updated":"Category created");setOpen(false)}catch(err:any){toast.error(err?.response?.data?.message||"Could not save category")}};return <><PageTitle title="Categories" description="Organize your BIGCROWN catalog." action={<button onClick={()=>{setEditing(null);setForm({name:"",slug:"",imageKey:""});setOpen(true)}} className="inline-flex items-center gap-2 rounded-xl bg-purple-600 px-4 py-3 text-sm font-bold text-white"><Plus className="size-4"/> Add category</button>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{isLoading?<div className="text-slate-400">Loading…</div>:data.map((c:any)=><div key={c._id} className="rounded-2xl border bg-white p-5"><div className="flex items-start justify-between"><div><h3 className="font-bold">{c.name}</h3><p className="mt-1 text-xs text-slate-400">/{c.slug}</p></div><button onClick={()=>edit(c)} className="rounded-lg p-2 hover:bg-purple-50 hover:text-purple-600"><Pencil className="size-4"/></button></div></div>)}</div>{open&&<div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/50 p-4"><form onSubmit={save} className="w-full max-w-md rounded-2xl bg-white p-6"><div className="mb-6 flex justify-between"><h2 className="text-xl font-bold">{editing?"Edit category":"Add category"}</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div><div className="space-y-4"><label className="block"><span className="mb-2 block text-sm font-semibold">Name</span><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="w-full rounded-xl border px-3 py-2.5"/></label><label className="block"><span className="mb-2 block text-sm font-semibold">Slug</span><input required value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})} className="w-full rounded-xl border px-3 py-2.5"/></label><label className="block"><span className="mb-2 block text-sm font-semibold">Image key / URL</span><input value={form.imageKey} onChange={e=>setForm({...form,imageKey:e.target.value})} className="w-full rounded-xl border px-3 py-2.5"/></label></div><div className="mt-7 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded-xl border px-4 py-2.5">Cancel</button><button className="rounded-xl bg-purple-600 px-5 py-2.5 font-bold text-white">Save</button></div></form></div>}</>}
